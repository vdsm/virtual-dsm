on:
  push:
    branches:
      - master
    paths:
      - '**/*.sh'

  pull_request:
    paths:
      - '**/*.sh'

name: "Test"
permissions: {}

jobs:
  check:
    uses: ./.github/workflows/check.yml
